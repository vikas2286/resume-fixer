# Frontend config (Vite injects VITE_* vars at build time).
# Point this at your backend if it doesn't run on localhost:8000.
# For deployment, set this to your DEPLOYED backend URL (e.g.
# https://resume-fixer-backend.onrender.com) on the static host at build time.
VITE_API_URL=http://localhost:8000

